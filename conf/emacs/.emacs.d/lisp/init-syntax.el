;;; init-syntax.el --- setup various syntax modes that doesn't need its own init file  -*- lexical-binding: t; -*-
;;; Commentary:

;;; Code:

(use-package vimrc-mode)


(provide 'init-syntax)
;;; init-syntax.el ends here
